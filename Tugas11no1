#include <iostream>
using namespace std;

int main() {
  int a, *bilangan;
  bilangan = &a;
  *bilangan = 200;

  cout << *bilangan << " " ;
  (*bilangan)++;
  cout << *bilangan << " " ;
  (*bilangan)--;
  cout << *bilangan << " \n ";

  getchar();
}
